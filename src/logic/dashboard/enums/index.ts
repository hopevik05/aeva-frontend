export enum DownloadStatus {
  onQueue = "on-queue",
  processing = "processing",
  failed = "failed",
  completed = "completed",
  cancelled = "cancelled",
}

export enum DashboardTabs {
  active = "ACTIVE",
  completed = "COMPLETED",
}
