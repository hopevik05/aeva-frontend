export enum AppEnv {
  "production" = "production",
  "staging" = "staging",
  "development" = "development",
}

export enum TrxState {
  unset,
  submit,
  success,
  error,
}

export enum Status {
  success = "success",
  completed = "completed",
  error = "error",
}
